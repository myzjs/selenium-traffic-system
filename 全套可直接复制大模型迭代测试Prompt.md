# 全套可直接复制大模型迭代测试Prompt（五层准入\+功能同步\+前后端数据精准）

## 【模板一】初次全量审计 Prompt（五层准入测试 · 最终版）

**使用场景**：新代码首次审计、完整体检、上线准入测试、所有按钮功能校验、前后端数据一致性校验

**强制五层准入测试（必须逐项输出结果）**

1\. 功能层：页面所有按钮、点击事件、输入框、开关、触发逻辑，必须与当前最新代码100%匹配，无失效、无残留、无废弃函数、无未绑定事件

2\. 数据层：前后端数据流转、参数传递、返回值接收、数据渲染、默认数据、空数据、异常数据，全部准确无误，不漏传、不错传、不丢失、不篡改

3\. 稳定性层：异步时序、竞态条件、定时器、请求重复触发、重复初始化、页面刷新/卸载稳定性、偶发bug扫描

4\. 异常容错层：网络失败、超时、空值、报错捕获、try\-catch完整性、无静默失败、无卡死阻塞

5\. 合规安全层：无违规逻辑、无风控风险、无内存泄漏、无无限循环、无重复绑定

**你的角色**：资深前后端测试工程师 \+ 代码审计工程师 \+ 上线准入评审官

**任务规则（严格执行，不得省略）**

1\. 不允许局部看代码，必须**全量通读全文**，所有功能、所有函数、所有按钮事件全部核对

2\. 所有页面按钮、交互功能必须和最新代码**完全同步匹配**，找出功能和代码不一致、代码更新但功能未更新、残留旧逻辑、冗余废弃代码

3\. 严格校验前后端数据：请求参数、回调数据、赋值逻辑、展示数据、状态数据**100%准确一致**

4\. 所有偶发、间歇性、概率性bug必须单独标注并解释根因

5\. 输出必须分级：高风险/中风险/低风险/优化点

6\. 必须输出：完整测试用例清单 \+ 修复根因 \+ 逐行改动说明 \+ **最终完整可运行代码**

**输出固定格式**

一、五层准入测试逐项结果（每层通过/不通过\+问题明细）

二、全部功能按钮校验清单（每一个按钮：功能状态、是否匹配代码、是否存在异常）

三、前后端数据一致性审计报告（参数、返回、渲染、异常场景全覆盖）

四、全部bug清单（含偶发bug根因）

五、完整修复方案

六、全套回归测试用例（可直接人工执行）

七、修复后完整最终代码

——————粘贴下方代码——————

【此处粘贴你的完整代码】

---

## 【模板二】二次独立复核 Prompt（防AI幻觉·防修旧出新·强制独立审计）

**使用场景**：第一次修复完必须跑一次，杜绝AI改出新bug、杜绝遗漏旧bug、确保功能和代码完全同步

**核心规则**：完全清空上一轮思考，**100%独立重新审计**，不继承、不参考上一轮修复逻辑

**审计重点**

1\. 检查是否残留原有所有bug

2\. 重点检查：修复过程是否**新增bug、破坏原有功能、破坏数据一致性、按钮功能失效**

3\. 核对所有按钮交互、事件绑定、触发逻辑是否与最新代码完全同步

4\. 复核前后端数据传入、传出、赋值、渲染准确性

5\. 深度排查间歇性偶发问题、时序竞态、内存泄漏、重复初始化

6\. 校验异常捕获、容错、稳定性是否达标上线准入

**输出要求**

1\. 复核通过 / 不通过 结论

2\. 所有隐藏问题、细微问题、兼容问题全部列出

3\. 如有问题：输出根因 \+ 精准修复 \+ 完整最终代码

4\. 无问题：输出【版本准入合格，可进入测试冻结】

——————粘贴修复后的完整代码——————

【此处粘贴代码】

---

## 【模板三】迭代修复专用 Prompt（解决越修越乱、反复bug、功能不同步）

**使用场景**：本地测试跑出新bug、偶发报错、功能异常、数据不对、按钮失效、迭代循环修复

**强制迭代规则**

1\. **禁止打补丁式修复**，必须定位根因，架构级解决，杜绝反复复发

2\. 必须保留所有原有正常功能，不得改好一处弄坏另一处

3\. 所有按钮功能、交互逻辑、数据流转必须持续与最新代码同步对齐

4\. 自动规避历史已修复bug，不重复踩坑

**本次迭代输入信息（已固定格式）**

1\. 当前完整最新源码：【粘贴完整代码】

2\. 本次实测bug现象：【粘贴现象】

3\. 完整控制台报错堆栈：【粘贴完整报错】

4\. 复现步骤 \+ 复现概率：【粘贴】

5\. 历史已经修复的bug列表：【粘贴】

**迭代修复输出要求**

1\. 精准根因分析（为什么反复出现、为什么偶发）

2\. 是否架构缺陷判断

3\. 最小改动、最稳修复、不影响其他功能

4\. 重新对齐所有按钮功能、前后端数据一致性

5\. 更新全套测试用例

6\. 输出最终完整可运行代码

——————开始迭代修复——————

## 【附赠：你的固定工作流口诀（永久有效）】

初次五层审计 → 本地实测拿报错 → 二次独立复核 → 有问题就迭代闭环 → 功能代码100%同步 → 版本冻结准入

> （注：部分内容可能由 AI 生成）
