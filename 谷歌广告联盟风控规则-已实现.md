# 谷歌广告联盟风控规则 — 已实现清单

> 更新时间：2026-07-31（P0-P2全量修复后）  
> 代码版本：app.py V2.5.8 (VPS 107.148.2.75:31141)  
> 专家评审综合评分：**89/100**（修复前80分）  
> 对照文档：《拨号VPS版流量自动化+双分支自测试完整部署方案（含谷歌广告风控攻防检测完整版）》

---

## 一、基础核心风控规则（PDF 第3节）

### 1.1 IP-指纹一致性 ✅ 已实现（阻断式）

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 时区一致性 | `COUNTRY_TIMEZONE_MAP`（20国映射）→ `get_timezone_for_country()` → 注入 `context_kwargs["timezone_id"]` | app.py L83-111 |
| 语言一致性 | `timezone_lang_map`（30+时区→语言映射）→ `generate_fingerprint()` 自动匹配 | app.py L7251-7266 |
| QA阻断校验 | `qa_log_fingerprint_ip_consistency()` 返回 `all_consistent`，不一致时 `continue` 拒绝该IP | app.py L7188-7248, L11045-11051 |
| UA合法性自检 | `_is_valid_ua()` 检测畸形UA，非法时回退到池中第一个合法UA | app.py L2312-2315 |

### 1.2 人类化行为模拟 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| ★ 多段三次贝塞尔鼠标轨迹 | `human_mouse_move()` 2-4段三次贝塞尔 + 垂直偏移S形轨迹 + Fitts定律步数 | app.py human_mouse_move |
| ★ 生理微颤 | 8-12Hz正弦叠加（幅度0.3-1.2px），速度钟形曲线调制 | app.py human_mouse_move |
| ★ 速度钟形曲线 | 先加速后减速，峰值在25%-40%处，两端慢中间快 | app.py human_mouse_move |
| 惯性滚动模型 | `simulate_human_in_window()` scroll action，衰减系数0.88，rAF驱动 | app.py simulate_human_in_window |
| 键盘输入 | bigram延迟表（常见字母组合快、罕见组合慢）+ 模拟删改思考 | app.py L3541+ |
| 文字选中/复制/粘贴 | `text_select` action：选中可见文本→Ctrl+C→偶尔Ctrl+V粘贴→Esc取消 | app.py simulate_human_in_window |
| 分心暂停 | 10%概率出现30-120秒“分心暂停”（模拟用户去倒水、看手机） | app.py L969-971 |
| 长会话疲劳模拟 | 超过120秒后疲劳系数0→1，动作间隔延长2.5倍、速度降低到40% | app.py simulate_human_in_window |
| 移动端触摸手势 | `touch_swipe`/`touch_tap`：检测移动UA后启用滑屏/轻触模拟 | app.py simulate_human_in_window |
| 高熵随机源 | `_secure_rng = secrets.SystemRandom()` 用于动作选择、任务间隔等关键参数 | app.py L1565 |
| 注意力热区模型 | 鼠标目标按热区权重采样：40%内容中心/20%导航/15%侧边栏/10%底部/15%随机 | app.py simulate_human_in_window |
| 阅读停顿 | 每3-5次鼠标移动后插入1-4秒停顿（模拟阅读） | app.py simulate_human_in_window |

### 1.3 IP复用隔离 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 24小时精确去重 | `ip_provider.py`: `IP_REUSE_INTERVAL = 24*3600`，`check_ip_used_recently()` + `record_ip_use()` | ip_provider.py |
| C段分散策略 | `C_SEGMENT_MAX_PER_WINDOW = 3`，同一/24子网6小时内最多3次 | ip_provider.py L523-575 |
| 动态IP生命周期 | IPDeep分配 → 有效5-10分钟 → 超时自动销毁 | ip_provider.py |

### 1.4 Cookie持久化 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| QA会话封存 | `prepare_qa_storage_state()` 按站点+国家加载/封存，7天过期归档 | app.py L1455-1471 |
| 广告Cookie清洗 | `_qa_filter_storage_state()` + `_qa_is_ad_cookie()` 自动清理广告域名Cookie | app.py L1419-1441 |
| storage_state注入 | `context_kwargs["storage_state"]` 注入持久化会话状态 | app.py context创建处 |

### 1.5 多元来路 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| ★ 流量来源多样化 | 60%搜索 + 20%直接访问 + 10%社媒跳转 + 10%外链跳转（可配置） | app.py 主流程 traffic_diversity |
| 动态Referer | `referer_mode: "dynamic"` 根据IP国别自动选择来源平台 | app.py L1687 |
| Referer来源页访问 | 第0步强制访问Referer来源页，确保 `document.referrer` 正确 | app.py L12630+ |
| 多搜索引擎 | 支持Google/Bing/DuckDuckGo等多引擎随机选择 | app.py perform_real_search |
| 社媒平台跳转 | Facebook/Twitter/Reddit/Pinterest/Instagram随机选择，停留3-8秒后跳转 | app.py 流量多样化模块 |
| 外链跳转 | HackerNews/Quora/Medium/StackOverflow随机选择，停留2-6秒后跳转 | app.py 流量多样化模块 |
| 视频Referer | udis视频使用任务级Referer，非udis视频独立选择 | app.py L8232-8242 |

### 1.6 随机间隔 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 任务间隔 | `_secure_rng.uniform(interval_cfg)` + 10%分心暂停（高熵随机源） | app.py L968-977 |
| 页面停留 | 对数正态分布 `math.exp(random.gauss(math.log(25), 0.5))` | app.py L3345 |
| RTT网络延迟抖动 | `simulate_rtt_jitter()` 对数正态分布，base_ms+jitter_ms，注入3个关键导航点 | app.py L170-180 |
| 广告点击后等待 | `random.uniform(2, 20)` 秒 | app.py L3325-3327 |
| 落地页停留 | 15-90秒对数正态分布 + 随机滚动 | app.py L3344-3357 |

---

## 二、攻防检测模块（PDF 第4节）

### 2.1 广告交互检测 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 广告曝光监控 | `scan_ads_during_task()` 全过程监控，≥50%可见面积判定 | app.py L2950-3170 |
| 有效曝光判定 | 累计≥50%可见且持续≥1秒（1000ms）= 有效曝光 | app.py L3131-3147 |
| 广告点击 | `try_click_visible_ad()` 概率掷骰 + 冷却 + 去重 | app.py |
| ★ 广告前停留≥8秒 | 检查`performance.timing.navigationStart`，页面停留<8秒时拒绝点击 | app.py try_click_visible_ad |
| ★ 广告前阅读模拟 | 移动鼠标到广告后先停留2-5秒（模拟阅读周围内容）+ 0.5-1.5秒犹豫 | app.py try_click_visible_ad |
| 每日点击计数 | `get_daily_ad_clicks()` / `record_ad_click()` 跨会话持久化 | app.py L2404-2416 |
| 落地页浏览 | 新标签页打开→滚动→停留15-90秒→关闭 | app.py L3340-3359 |
| 点击冷却 | 3分钟全局冷却 + 50px粒度位置去重 | app.py _ad_click_cooldown |

### 2.2 IP溯源检测 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| IP信息解析 | `ip_info_resolver.py` 完整解析IP→国家/城市/时区/语言/ISP | ip_info_resolver.py |
| IP类型检测 | 识别 datacenter/proxy/vpn/hosting，高危类型**自动拒绝**（阻断式） | app.py L10342-10350 |
| ★ TLS/JA3指纹伪装 | `curl_cffi` 模拟Chrome TLS指纹，`tls_safe_get()`/`tls_safe_post()` 包装所有外部HTTP请求 | app.py 顶部 tls_safe_* |
| WebRTC防泄露 | `init_script` 包装 `RTCPeerConnection` + 过滤 ICE candidate | app.py init_script |
| 代理bypass | `--proxy-bypass-list=safebrowsing.googleapis.com;...` 防止Google安全浏览泄露真实IP | app.py 启动参数 |

### 2.3 JS探针检测 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 指纹生成 | `generate_fingerprint(ip_info)` 根据IP信息动态生成完整指纹 | app.py L7251+ |
| 浏览器属性注入 | timezone_id / locale / viewport / device_scale_factor / is_mobile / has_touch | app.py context_kwargs |
| Geolocation坐标注入 | `get_geolocation_for_ip()` + `CITY_COORDINATES` 城市→经纬度映射 + ±0.05°随机抖动 | app.py CITY_COORDINATES |
| permissions授权 | `permissions=["geolocation"]` 主动授权地理位置API | app.py context_kwargs |
| ★ Canvas噪声(xorshift128) | 逐像素微噪声（2%像素±1），使用xorshift128 PRNG替代LCG（防ML识别） | app.py init_script |
| ★ AudioContext噪声 | OfflineAudioContext.startRendering + AnalyserNode.getFloatFrequencyData/getByteFrequencyData | app.py init_script |
| ★ WebGL时间侧信道 | performance.now()累积漂移 + readPixels随机干扰（防GPU时间指纹） | app.py init_script |
| ★ 事件时序对齐 | rAF帧同步 + Event.timeStamp量化到16.67ms帧边界 | app.py init_script |
| ClientRects微噪声 | getBoundingClientRect ±0.4px噪声（防DOM布局指纹） | app.py init_script |
| 字体枚举限制 | document.fonts.check白名单 + measureText宽度噪声 | app.py init_script |
| toString中心伪装 | 所有hook函数均返回`function xxx() { [native code] }` | app.py init_script |

### 2.4 流量链路检测 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 完整访问链路 | Referer来源页 → 搜索引擎 → 搜索结果 → 目标站 → 广告点击 → 落地页 | app.py 主流程 |
| 自然跳转 | 从来源页通过JS导航，`document.referrer` 自动正确 | app.py L12782+ |
| RTT注入点 | 搜索引擎主页(80ms±40ms)、Referer来源页(60ms±50ms)、目标站跳转(100ms±60ms) | app.py 多处 |
| 搜索行为 | 真搜索流程：点击搜索框→清空→分段输入关键词→等待结果→点击链接 | app.py L3477+ |

### 2.5 时序检测 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| 工作时间调度 | `is_working_hours()` 当地时间7:00-24:00，非工作时段降频10% | app.py L113-143 |
| 全球24h分布 | `generate_daily_tasks()` 按各国工作时段UTC转换，24小时全球分布 | app.py L634+ |
| 凌晨降频 | 0-7点当地时段仅10%概率执行 | app.py L521-528 |
| 任务计划生成 | 按网站属性/日流量/随机模型自动生成多天任务 | app.py L1074+ |

### 2.6 账号/会话隔离 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| QA会话隔离 | 按站点+国家隔离 `qa_sessions/{site}/{country}/` | app.py L1407-1410 |
| 会话封存 | 7天过期自动归档到 `archive/` 子目录 | app.py L1465-1471 |
| Cookie域名过滤 | `_qa_cookie_domain_matches()` 严格匹配，只保留目标站Cookie | app.py L1413-1416 |
| 广告Cookie清洗 | 自动识别并清除 doubleclick/google 等广告域名Cookie | app.py L1419-1424 |
| ★ 跨会话行为画像 | `behavior_profiles/{site}_{country}.json` EMA记忆（停留/滚动深度/点击热区） | app.py load/save_behavior_profile |
| ★ 行为一致性应用 | 根据历史画像微调本次任务参数（停留时长/滚动深度），制造用户级一致性 | app.py apply_behavior_profile_to_config |

### 2.7 反向检测 ✅ 已实现

| 项目 | 实现方式 | 代码位置 |
|------|----------|----------|
| Cloudflare绕过 | `watch_video_ad()` 中检测并处理CF验证挑战 | app.py L8244+ |
| UA池24h去重 | `UAPoolManager` 24小时窗口UA不重复 | app.py L1978-1982 |
| UA变体生成 | 每个基础UA生成3-5个随机变体（Chrome版本微调） | app.py L2200-2205 |
| fake_useragent集成 | 第三方库生成真实UA，混合到UA池 | app.py L2207-2229 |

---

## 三、双分支自测适配（PDF 第5节）

| 项目 | 实现方式 | 状态 |
|------|----------|------|
| 网站分支 | `website` 任务：搜索引擎→目标站→广告曝光/点击 | ✅ 已实现 |
| 视频分支 | `video` 任务：视频广告观看 + udis中转方案 | ✅ 已实现 |
| 混合调度 | `qa_run_phases.website` / `qa_run_phases.video` 独立开关控制 | ✅ 已实现 |

---

## 四、仍缺失项（按优先级）

| 优先级 | 缺失项 | 说明 |
|--------|--------|------|
| P1 | reCAPTCHA/hCaptcha交互 | 需要接入第三方打码平台（2Captcha/Anti-Captcha），涉及付费服务 |
| P3 | TCP/IP协议栈指纹 | 需真实代理链路或内核级修改（结构性限制） |
| P3 | HTTP/2 SETTINGS帧指纹 | 需定制HTTP/2客户端库 |
| P3 | 浏览器内核定制 | 编译Chromium源码去除所有自动化痕迹 |
| P4 | 真实设备农场 | 物理设备+真实网络，终极方案 |

> 注：原P0-P2缺失项已全部修复（TLS/JA3、流量多样化、AudioContext、鼠标轨迹ML对抗、广告前停留、WebGL时间侧信道、事件时序、行为画像）。

---

## 五、关键参数速查表

| 参数 | 值 | 说明 |
|------|-----|------|
| `IP_REUSE_INTERVAL` | 24小时 | 同IP去重间隔（ip_provider.py实际生效） |
| `C_SEGMENT_MAX_PER_WINDOW` | 3次/6小时 | 同C段分散限制 |
| 工作时段 | 7:00-24:00 | 各国本地时间 |
| 凌晨降频 | 10% | 0:00-7:00 |
| QA会话有效期 | 7天 | 过期自动归档 |
| 广告有效曝光 | ≥50%可见 + ≥1秒 | AdSense标准 |
| ★ 广告前最少停留 | 8秒 | `ad_min_page_stay` 配置 |
| ★ 广告前阅读模拟 | 2-5秒 | 移动鼠标到广告后的停留 |
| RTT基础延迟 | 60-100ms | 3个注入点各不同 |
| 分心暂停概率 | 10% | 30-120秒 |
| 落地页停留 | 15-90秒 | 对数正态分布 |
| 疲劳模拟阈值 | 120秒 | 超过后疲劳系数0→1 |
| 疲劳最大倍数 | 2.5倍 | 动作间隔延长上限 |
| 高熵随机源 | `secrets.SystemRandom()` | 动作选择/任务间隔 |
| Geolocation精度 | ±0.05° | 模拟GPS误差 |
| ★ TLS指纹 | Chrome (curl_cffi) | JA3哈希匹配Chrome 120+ |
| ★ 流量分布 | 60/20/10/10 | 搜索/直接/社媒/外链 |
| ★ 鼠标微颤频率 | 8-12Hz | 人手自然震颤 |
| ★ 鼠标轨迹分段 | 2-4段 | 三次贝塞尔，距离自适应 |
| ★ Canvas PRNG | xorshift128 | 替代LCG，防ML识别 |
| ★ 事件时序对齐 | 16.67ms帧边界 | 60Hz rAF同步 |
| ★ 行为画像EMA | α=0.3 | 新数据占30%权重 |

---

## 六、专家评审评分记录

| 专家 | 修复前 | 修复后 | 提升 |
|------|--------|--------|------|
| 自动化检测 | 78 | **90** | +12 |
| 浏览器指纹 | 82 | **91** | +9 |
| 网络/IP溯源 | 75 | **85** | +10 |
| 行为分析 | 80 | **90** | +10 |
| 广告合规 | 85 | **89** | +4 |
| **综合** | **80** | **89** | **+9** |
